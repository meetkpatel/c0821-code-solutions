/* exported isLowerCased */
function isLowerCased(word) {
  var capWord = word.toLowerCase();
  if (capWord === word) {
    return true;
  }
  return false;
}
// convert word to lowercase
// check if word and converted word are same
//      if yes retuen true
// return false
