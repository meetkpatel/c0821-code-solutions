/* exported getLastChar */
function getLastChar(string) {
  var stringLastIndex = string.length - 1;
  return string[stringLastIndex];
}

// storage for string last index
// get length of string
// -1 from string length to get actual index
// store output variable and return string at storage variable
