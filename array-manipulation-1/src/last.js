/* exported last */
function last(array) {
  var lastIndex = array.length - 1;
  return array[lastIndex];
}
// create storage for array last index value
// find the length of array - 1 for the last array index and store it to the storage
// return array at storage value
